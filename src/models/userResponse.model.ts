export interface UserResponse {
  firstName: string;
  lastName: string;
  id: number;
  phoneNumber: string;
  email: string;
  address: string;
  additionalInfo?: string;
  avatar: string;
  // eslint-disable-next-line semi
}
